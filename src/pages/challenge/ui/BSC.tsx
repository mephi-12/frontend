import { SatchelCryprosystemBackdoorTask } from '@shared/api/satchelCryprosystemBackdoor/types'
import React from 'react'

export const BSC = ({task}: {task: SatchelCryprosystemBackdoorTask}) => {
  return (
    <div>BSC</div>
  )
}
